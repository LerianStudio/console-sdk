{
  "name": "sindarian-server-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "cd .. && npm run build",
    "dev": "npm run prebuild && ts-node index.ts",
    "pretest": "npm run prebuild",
    "test": "jest"
  },
  "dependencies": {
    "@lerianstudio/sindarian-server": "file:../",
    "inversify": "^7.7.0",
    "next": "^15.4.0",
    "reflect-metadata": "^0.2.2",
    "zod": "^4.0.0"
  },
  "devDependencies": {}
}
