import { Meta, Controls, Primary, Canvas } from '@storybook/addon-docs/blocks'
import * as Story from './sidebar.stories'

<Meta of={Story} />

# Sidebar

The main application sidebar, it features automatically active items by path, disabled states and collapsible behaviour.

<Canvas className="light" of={Story.Primary} />

<Primary />
<Controls />
