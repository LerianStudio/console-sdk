import { Meta, Controls, Primary, Canvas } from '@storybook/addon-docs/blocks'
import * as Story from './select.stories'

<Meta of={Story} />

# Select

Displays a list of options for the user to pick from—triggered by a button.

### Default

<Canvas of={Story.Default} />

### Read Only

Disables the component's user interactions while keeping the DOM events enabled. Useful when using together with Tooltip or other components
that relies on JavaScript events.

<Canvas of={Story.ReadOnly} />

### Disabled

<Canvas of={Story.Disabled} />

<Controls />
